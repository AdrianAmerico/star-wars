import { Theme } from '@emotion/react'

export const styles: Theme = {
  display: 'flex',
  position: 'absolute',
  inset: '0',
  flexDirection: 'column',
  alignItems: 'center',
  justifyContent: 'center'
}
